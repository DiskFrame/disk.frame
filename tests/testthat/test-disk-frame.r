context("test-disk.frame")

test_that("test add_meta", {
  # it works so how to test this?
  
  
})

test_that("test head", {
  # it works so how to test this?
  
  
})

test_that("test tail", {
  # it works so how to test this?
  
  
})


test_that("test nrow", {
  # it works so how to test this?
  
  
})

test_that("test ncol", {
  # it works so how to test this?
  
  
})